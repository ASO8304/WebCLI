def process_command(command: str, username: str) -> str:
    command = command.strip().lower()
    
    if command == "help":
        return "Available commands: status, help, exit"
    elif command == "status":
        return "📊 System status: Read-only summary available."
    else:
        return f"❓ Unknown or unauthorized command: {command}"
